sudo: false
language: node_js
node_js:
  - '8'
  - '6'
  - '4'
after_success: npm run coverage
cache:
  directories:
  - bin
  - node_modules
addons:
  apt:
    packages:
      - oracle-java8-set-default
script: travis_wait 60 xo --space && ava --verbose
